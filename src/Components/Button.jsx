import React from "react";

const Button = ({ label }) => {
  return <button className="btn border-0 button-common px-8 rounded-lg cursor-pointer text-black font-bold">{label}</button>;
};

export default Button;
